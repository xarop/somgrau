name: Deploy theme via FTP

on:
  push:
    branches:
      - main
    paths:
      - 'wp-content/themes/somgrau/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./wp-content/themes/somgrau/
          server-dir: wp-content/themes/somgrau/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
